# Two API endpoints
# 1. POST /upload: Accepts a file upload, processes it, and stores the relevant information in the database.
# 2. POST /query: Accepts a user query, retrieves relevant information from the database, and returns the response.
# Should handle CORS so frontend can communicate with the backend without issues.